{
    "cluster": "CLUSTERNAME",
    "service": "SERVICENAME",
    "desiredCount": TASK_COUNT,
    "taskDefinition": "TASK_FAMILY:REVISION",
    "deploymentConfiguration": {
        "maximumPercent": SERVICE_TASKCOUNT_MAX,
        "minimumHealthyPercent": SERVICE_TASKCOUNT_MIN
    }
}
